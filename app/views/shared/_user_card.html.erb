<div class="ui card">
  <div class="image">
    <img src="<%= @user.user.profile_image_url %>">
  </div>
  <div class="content">
    <a class="header"><%= @user.user.name %></a><p> @<%= @user.user.screen_name %></p>
    <div class="meta">
      <span class="date">Joined in <%= @user.user.created_at.strftime("%Y") %></span>
    </div>
    <div class="description">
      <%= @user.user.description %>
    </div>
  </div>
  <div class="extra content">
    <%= @user.user.followers_count %> Followers | <%= @user.user.friends_count %> Following
  </div>
</div>
